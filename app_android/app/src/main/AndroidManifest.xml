<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="propya.mr.jeevan">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        >
        android:theme="@style/AppTheme">
<!--        <activity android:name=".Activities.KnowTheHospital"></activity>-->
        <activity android:name=".Activities.HospitalFinderActivity"></activity>
        <activity android:name=".AmbulanceRequest"></activity>
<!--        <activity android:name=".Activities.KnowTheHospital" />-->
        <activity android:name=".Activities.ConfirmedInfo"></activity>


        <activity android:name=".ChatBot.ChatBotMain" />
        <activity android:name=".SOS.SOSActivitiy" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ChooseEmergencyActivity" />

        <service
            android:name=".Services.FCMBase"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>